<Window x:Class="Main.Integration.IngLockList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Main.Integration"
        mc:Ignorable="d"
        Title="互锁列表" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>
        <DataGrid Name="lvRecord" FontSize="20" FontFamily="微软雅黑" AutoGenerateColumns="False" ScrollViewer.VerticalScrollBarVisibility="Visible">
            <DataGrid.Columns>
                <DataGridTextColumn Header="序号" Width="70" Binding="{Binding ID}"/>
                <DataGridTextColumn Header="内容" Width="200" Binding="{Binding Content}"/>
                <DataGridTextColumn Header="操作" Width="200" Binding="{Binding Opr}"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
